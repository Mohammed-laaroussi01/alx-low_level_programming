#include "main.h"

/**
 * add -  adds two integers and returns the result.
 * @a: input to check
 * @b: input to check
 * Return: the sum of result
 */

int add(int a, int b)
{
	int k;

	k = a + b;
	_putchar('0' + k);

	return (k);
}
