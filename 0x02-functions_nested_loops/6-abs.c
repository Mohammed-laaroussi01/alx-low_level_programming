#include"main.h"

/**
 * _abs - computes the absolute value of an integer.
 *
 * @int: the int to check
 * Return: the abs value of int
 */

int _abs(int n)
{
	if (n >= 0)

		return (n);

	else
		return (-n);
}

